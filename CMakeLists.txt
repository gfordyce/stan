project(stan)
set (STAN_DIRS model view controller utils)
add_definitions(-fPIC -Wall -g `wx-config --cxxflags`)
include_directories(. model view controller utils /opt/boost/include/boost-1_35)
link_directories(/opt/boost/lib)
link_libraries(model view controller utils boost_thread-gcc42-mt `wx-config --libs`)
# target_link_libraries(stanlib boost_thread_gcc42-mt)
#for $dir in STAN_DIRS:
#    add_subdirectory(${dir})
add_subdirectory(model)
add_subdirectory(view)
add_subdirectory(controller)
add_subdirectory(utils)
